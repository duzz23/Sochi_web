import event.models
import home.models
from django.urls import include, path

urlpatterns = [

    # path("", event.models.OrganizersList.as_view(), name="organizers"),
    # path("", home.models.PrivacyPolicy.as_view(), name="privacy-policy"),
    ]